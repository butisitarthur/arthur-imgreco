# Application Settings
APP_NAME=Arthur Image Recognition
APP_VERSION=2.0.0
DEBUG=false
LOG_LEVEL=INFO

# Redis Configuration  
REDIS_URL=redis://localhost:6379

# Qdrant Vector Database
QDRANT_URL=http://localhost:6333

# Note: Port configurations (APP_PORT, REDIS_PORT, QDRANT_PORT, etc.) are handled in docker-compose.yml

# ML Model Configuration
CLIP_MODEL_NAME=ViT-B/32
EMBEDDING_DIMENSION=512
MAX_IMAGE_SIZE=1024

# API Configuration
API_V1_STR=/api/v1
CORS_ORIGINS=["*"]

# Note: MAX_UPLOAD_SIZE not implemented yet - file uploads use FastAPI defaults

# Note: Monitoring ports (Prometheus, Grafana, Nginx) are configured in docker-compose.yml
# Note: No authentication system implemented yet - JWT/auth variables removed

# Performance Settings
MAX_WORKERS=4
BATCH_SIZE=32

# Note: INDEX_BUILD_CONCURRENCY not implemented yet